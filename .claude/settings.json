{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *apps/frontend/app/*|*apps/frontend/components/*) echo \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"allow\\\",\\\"additionalContext\\\":\\\"Reminder (CLAUDE.md Design source of truth): before writing $f, check the matching page in design/static_html/*.html for exact layout/copy/interaction. If no mockup exists for this route/component, proceed but flag the gap explicitly rather than guessing.\\\"}}\" ;; esac; }"
          }
        ]
      }
    ]
  }
}
